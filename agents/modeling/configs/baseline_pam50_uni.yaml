task: pam50
username: sjpark
embedding_model: uni
embedding_dim: 1024
num_classes: 5   # LumA=0, LumB=1, Basal=2, HER2=3, Normal=4

model:
  hidden_dims: [512, 256]
  dropout: 0.3

train:
  epochs: 10
  lr: 1.0e-3
  batch_size: 1
  seed: 42

data:
  embedding_manifest: /workspace/data/cache/biop02/embedding_manifest_uni.csv
  label_col: pam50
  n_dummy_slides: 20

output_dir: /workspace/agents/modeling/experiments
